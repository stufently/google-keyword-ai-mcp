# M18 test fix round implementation plan

> **For agentic workers:** Execute inline using superpowers:executing-plans.

**Goal:** Close the four test gaps and kill ten demand mutants.

**Architecture:** Keep production bytes unchanged. Extend algorithm and mocked
HTTP tests; let each mutation select its source and test file.

**Tech Stack:** Python 3.14, pytest, respx, existing `.venv`.

**Spec:** `docs/specs/m18-fix-round.md` and `docs/specs/m18-anchored-demand.md`.

## Global constraints

- Stay in this clone on `m18-anchored-demand`; one new commit over `1eab8f5`.
- No push, Docker, dependency installation, production or fixture changes.
- Preserve existing tests and the nine report criterion IDs.
- Run mutations sequentially with byte and SHA-256 restoration.

## Task 1: Pin the algorithm contracts

**File:** `tests/test_demand.py`.

- [x] Add two successful batches with two versus one whole weeks. Check
  `assert (anchor.batch, anchor.weeks, anchor.measured_weeks) == (1, 2, 2)`.
- [x] Add success followed by provider failure; check the anchor remains
  `100.0` from batch 1 and the failed participant has `None` and the exact reason.
- [x] Add `assert plan_batches(["a", "b"], "a") == [["a", "b"]]`.
- [x] Run `.venv/bin/python -m pytest -q tests/test_demand.py`.

## Task 2: Pin caveats and all three default entry points

**File:** `tests/test_demand_usecase.py`.

- [x] Replace the length assertion with equality to the three imported caveats.
- [x] Add a dedicated test comparing all three constants to their literal text;
  this independently detects a constant mutation.
- [x] Add separate usecase, CLI and MCP tests omitting `timeframe`, exercising
  the actual provider through `respx`. Assert output timeframe and the explore
  request's `comparisonItem[0]["time"]` both equal `"today 12-m"`.
- [x] Run `.venv/bin/python -m pytest -q tests/test_demand_usecase.py`.

## Task 3: Extend and verify the mutation gate

**File:** `tests/mutation_gate_demand.py`.

- [x] Add per-mutation source and test paths with existing paths as defaults;
  use them in anchor checking, execution, failure location and restoration.
- [x] Add M8 (`elif True:`), M9 (absolute-volume caveat), M10 (`today 5-y`)
  with distinct target tests and literal assertion lines.
- [x] Run `--self-check` before the full gate. Require ten kills and unchanged
  SHA-256 for both production files.

## Task 4: Deliver verified results

**Files:** `report.json`, supplied fix-round spec, this plan, the three test files.

- [ ] Run AC-001 through AC-009 exactly as specified, checking tree cleanliness
  after the single commit. Update `report.json` with actual results.
- [x] Review the diff and compare all production and fixture bytes with the base.
- [ ] Commit only task files plus the supplied task spec; keep `report.json`
  untracked as allowed by AC-004. Do not push.

Final commit and post-commit criterion results are recorded in `report.json`;
the delivery checkboxes above describe actions performed after saving this plan.
