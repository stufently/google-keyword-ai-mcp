# Рабочий журнал

Один источник правды по СТАТУСУ работы. Изменения кода и поведения — в
`CHANGELOG.md`, устройство подсистем — в `docs/`. Здесь не дублировать ни то,
ни другое.

## IN_PROGRESS

- **M14 — верхняя граница объёма кеша.** Спека `docs/specs/m14-cache-size-limit.md`
  (уже на main, коммит `f4a131e`). Исполнитель Codex ОТРАБОТАЛ, приёмка НЕ
  завершена.

  **Где лежит работа:** клон `/home/deploy/github/google-keyword-ai-mcp-codex`,
  ветка `m14-cache-size-limit`, коммит с работой — `c96b512`
  «feat: bound cache size with LRU eviction» (10 файлов, ровно те, что
  разрешала спека). Каталог прогона
  `/home/deploy/.cache/codex-build/google-keyword-ai-mcp-codex/20260904T134600Z-resume`.

  ⚠️ **Забирать ТОЛЬКО `c96b512`, черри-пиком, а не ff-мержем.** Два других
  коммита ветки (`9bb1957`, `c0d6fa3`) несут блок `codex-build:begin` в
  `AGENTS.md` — это артефакт обвязки, на main ему не место (в M13 он так и
  уехал, пришлось откатывать). Спека уже на main отдельным коммитом.
  Команда: `git cherry-pick c96b512`, затем проверить
  `git diff | grep codex-build:begin` — должно молчать.

  **Что уже проверено:**
  - Гейт: 11/12 `pass`. Единственный `fail` — агрегирующий AC-012, остановка по
    контракту: в песочнице Codex зависает in-memory MCP-вызов в
    `tests/test_facade_errors.py::test_a_missing_run_is_an_answer_over_mcp_not_a_tool_failure`
    (основной поток в `selectors.select`, воркер AnyIO в `queue.get`, стек вне
    изменённых файлов). Это среда, а не регресс: в M13 тот же тест зелен на
    хосте за 9,6 с и в Docker на обеих версиях.
  - Границы: изменены ровно 10 разрешённых файлов, `pyproject.toml` и
    `uv.lock` не тронуты.

  🚨 **НАЙДЕН НАСТОЯЩИЙ РЕГРЕСС, чинить первым.** Полный прогон в Docker на
  3.14: `1 failed, 495 passed`. Падает СУЩЕСТВУЮЩИЙ тест
  `tests/test_storage.py::test_pragma_and_cache_schema_are_created` — на
  строке 38 он сверяет набор колонок `cache_entries` на РАВЕНСТВО, а веха
  добавила `last_read_at`. Починка — одна строка: дописать `"last_read_at",`
  в ожидаемое множество.
  **Причина дефекта — в МОЕЙ спеке:** `tests/test_storage.py` не был включён
  в список разрешённых файлов, поэтому исполнитель не мог его поправить, а
  увидеть падение не мог из-за зависшего агрегата. Это дословное повторение
  урока M12 («спека запрещала трогать файл, где лежала половина причины»):
  границы вехи надо сверять с тем, что веха ломает, ДО запуска. В частности,
  добавление колонки ВСЕГДА задевает тест, сверяющий набор колонок на равенство.

  **Что осталось сделать по приёмке:**
  1. Починить `tests/test_storage.py` (одна строка) — иначе набор красный.
  2. Полный прогон в Docker на 3.12 (на 3.14 сделан, см. выше).
  3. Мутационная проверка по HC-005 спеки, семь мутантов; главный — (ж):
     вернуть вытеснение в ветку «один раз на экземпляр», обязан упасть AC-007.
  4. Живой прогон HC-003: база версии 3 С ДАННЫМИ уже снята ДО вехи и лежит в
     `/tmp/m14-v3-fixture/gkai.sqlite3` (5 записей кеша, 1 прогон,
     `user_version=3`) — открыть её новым кодом и убедиться, что доезжает до
     версии 4 без потери прогонов.
  5. HC-004: валидатор отвергает отрицательный `cache_max_bytes`,
     `SCHEMA_VERSION` равен 4, база версии 5 по-прежнему отвергается.
  6. Забрать черри-пиком, обновить CHANGELOG/TASKS, запушить.

## COMPLETED (2026-09-04)

- **M13 — просроченный кеш перестаёт накапливаться.** Спека
  `docs/specs/m13-cache-retention.md`, исполнитель Codex, влито в `0db0f82`.
  `SqliteCache.purge_expired/purge_all/counts`, подметалка при первой записи,
  команды `gkai cache status` и `gkai cache purge [--all] [--vacuum]`.

- **M11 — живые опт-ин тесты неофициальных провайдеров.** Спека
  `docs/specs/m11-live-integration-tests.md`, исполнитель Grok, влито в
  `8127f6b`. `tests/test_live_providers.py` ходит в настоящий Google;
  запускать явно: `pytest -m integration` (в дефолтный прогон не попадает).
- **M12 — одновременный старт не роняет открытие базы.** Спека
  `docs/specs/m12-concurrent-migrations.md`, исполнитель Codex, влито в
  `40a8a2d`.

## Что стоит знать следующей сессии

- **AC-005 в спеке M13 был ложным критерием, и это моя ошибка, а не
  исполнителя.** Он объединял три утверждения в одном выражении `-k` и
  отчитался `pass`, хотя оба выключателя проверялись впустую. Мутации,
  пережившие весь набор: игнорировать `cache_sweep_enabled` целиком — 30
  тестов зелены; подмести ДО проверки `cache_enabled` — 16 зелены; убрать
  `logger.warning` из best-effort — 16 зелены. Код при этом ПРАВИЛЬНЫЙ:
  дырявы тесты, а не поведение. Закрыть в следующей вехе — она трогает тот же
  `set()`.
- **Один критерий — одно утверждение.** Связка нескольких проверок через
  `-k "a or b or c"` даёт зелёный, когда сработала хотя бы часть, и гейт
  этого не видит.
- **Приёмка ловит то, чего не ловит гейт.** Гейт дал 11/12; четыре мутанта из
  моего первого набора честно умерли, а второй набор (по находкам ревьюера)
  выжил целиком. Мутационная проверка чужих тестов — не формальность.
- **Живой прогон подтвердил смысл вехи:** `gkai cache purge --all --vacuum`
  уменьшил файл с 372 736 до 159 744 байт, сохранённый прогон
  `run_516000d54a6f4deaa309aa2eb6` остался цел.

- **Приёмка чужой работы обязательна и не сводится к коду возврата.** У M11
  машинный гейт отверг отчёт (проза вместо JSON), у M12 исполнитель честно
  остановился по контракту без коммита. В обоих случаях работа была годной, но
  доводил её принимающий.
- **Мутационная проверка окупилась дважды.** У M11 выжил мутант «рынок
  игнорируется»: тест искал кириллицу в ответе на кириллический запрос и потому
  ничего не проверял. У M12 три выживших мутанта указали на мёртвый код в
  поставленном решении.
- **У WAL-половины гонки M12 надёжного теста нет, и это записано честно.**
  Многопроцессный тест ловит возвращённую поломку примерно в половине случаев;
  прицельный тест на механизм не поймал её вовсе и был удалён. Доказательство —
  замер в комментарии к тесту: с дефектом 3 падения из 20, после — 0 из 25.
  Если кто-то придумает детерминированный способ воспроизвести отказ
  `PRAGMA journal_mode=WAL`, это будет настоящим улучшением.
- **Спека M12 была с дефектом:** она запрещала трогать `storage/engine.py`, где
  и находилась половина причины. Проверять границы вехи против места дефекта
  ДО запуска, а не после.

## Кандидаты на следующие вехи

Ни один не исследован до уровня спеки — перед запуском обязателен шаг 1
маршрута (проверить предпосылки вживую), иначе спека унаследует чужую догадку.

- Пагинация Google Ads не ограничена: `_call_ideas` вычерпывает весь pager под
  лимитом 1 rps, а `max_ads_calls` считает это одним вызовом. Задокументировано
  как осознанное решение в `docs/pipeline.md`, так что сначала решение, потом
  спека.
