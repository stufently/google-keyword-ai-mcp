# Рабочий журнал

Один источник правды по СТАТУСУ работы. Изменения кода и поведения — в
`CHANGELOG.md`, устройство подсистем — в `docs/`. Здесь не дублировать ни то,
ни другое.

## С чего начать следующей сессии

Сессия 05.09.2026 закрыта по стоимости контекста, а не по незавершённой работе:
оба репозитория чисты и запушены, вехи в очереди нет. Правила проекта здесь НЕ
дублируются — они в `CLAUDE.md` (устройство) и `AGENTS.md` (прогон, Docker,
запреты); правила хоста — в `~/.claude/CLAUDE.md`.

- **Неслитых веток НЕТ** (проверено 06.09.2026, см. COMPLETED). Вся история
  `main` линейна, 106 коммитов, ни одного merge-коммита — так и держать.

- **Ждём ровно одного: кредов Google Ads.** Пять переменных перечислены ниже в
  IN_PROGRESS. Пока их нет, запускать по Ads нечего — ни вехи, ни замера.
- **Как только креды появятся — сначала замер, потом решение, и только потом
  спека.** Мерить надо ОДНО число: сколько страниц реально отдаёт
  `generateKeywordIdeas` на боевых запросах (см. «Кандидаты» — там записано,
  почему именно это число решает, нужна веха или нет). Спеку до замера не
  писать: текущее поведение задокументировано в `docs/pipeline.md` как
  осознанное решение, и менять его без данных — значит унаследовать догадку.
- **GSC проверяется вживую без новых кредов.** Разрешение владельца от
  05.09.2026 (TG 9969): можно брать его gcloud-ADC
  `~/.config/gcloud/application_default_credentials.json` (тип
  `authorized_user`, read-only). Нужен ещё `GKAI_SEARCH_CONSOLE_QUOTA_PROJECT_ID`
  — без него 403, это и чинила M15. Путь к файлу передавать ТОЛЬКО переменной
  окружения процесса: ни в репозиторий, ни в отчёты он не попадает.
- **Правки скилов `codex-build`/`grok-build` за 05.09 лежат в `~/github/ansible`**
  (коммиты `821c1fa`, `bb86a8d`, `a0a8aa8`) и описаны в CHANGELOG ТОГО репо —
  сюда их не переписывать. Живая копия в `~/.claude/skills/` — деплой-артефакт.
- **Прогон, оборванный на `Selected model is at capacity`, — не провал работы.**
  Так дважды умерла M15: код и тесты были дописаны, отчёта не было, гейт дал ❌
  «отчёта нет». Смотри `codexlog.py trace` — если команды критериев в логе
  зелёные, принимай руками, а не гоняй третий заход.

## IN_PROGRESS

**M19 — порог достоверности покрытия у относительного спроса.** Запущена
06.09.2026, исполнитель Grok, панель `gk-gkai-m19-coverage`, клон
`/home/deploy/exec-clones/gkai-m19-coverage`, ветка `m19-demand-coverage`, спека
`docs/specs/m19-demand-coverage.md`, 9 критериев. Закрывает дефект, записанный
при приёмке M18: значение по одной измеренной неделе из 53 стоит в столбце так
же уверенно, как по 53 из 53. Настройка `GKAI_DEMAND_MIN_COVERAGE` (доля, дефолт
0.25); ниже порога строка едет `null` с причиной, называющей покрытие.

- **Порог — ДОЛЯ, а не число недель**, потому что окно задаётся `--timeframe`:
  абсолютные восемь недель из `scoring.trend_growth_gap` тут не годятся, там
  окно сравнения фиксировано.
- **Якоря порог не касается**: якорь определяет шкалу, обнулить его — выбросить
  весь пакет, а этот случай уже описан обвалом якоря.
- Порядок вех выбран так намеренно: интеграция столбца в `research` идёт
  СЛЕДУЮЩЕЙ, после порога. Вставлять в сценарий столбец, которому нельзя
  верить, хуже, чем не вставлять.
- Перекрёстные мутации на приёмке гоняет Codex — M18 писал он, M19 пишет Grok.
- **Спека дополнена до начала работы: различение живёт в СХЕМЕ, а не в прозе.**
  У `DemandRow` появляется машинный `status` из пяти значений (`measured`,
  `low_coverage`, `below_resolution`, `anchor_collapsed`, `batch_failed`).
  Без него порог менял бы одну неразличимость на другую: сегодня «мало данных»
  и «спрос низкий» разводит только текст `reason`, то есть потребителю пришлось
  бы сравнивать строки — ровно та ошибка формы, ради которой писалась M18.
  Низкий спрос остаётся ЧИСЛОМ (включая измеренный `0.0`), нехватка данных —
  пустым значением со своим `status`. Панель перезапущена: работа ещё не
  начиналась, терять было нечего.

## COMPLETED (2026-09-06)

- **M18 — сопоставимый спрос по всему набору через якорный пересчёт Trends.**
  Исполнитель Codex, перекрёстное ревью Grok, влито в `main` (`51df0d1`,
  `acf60c6`). `gkai demand` и MCP-инструмент `rank_keyword_demand`: 2–50 ключей
  бьются на пакеты «якорь + до четырёх», каждый пакет делится на среднее ЯКОРЯ в
  этом же пакете, все пакеты встают на шкалу «якорь = 100». Тестов 554 → 602.
  - **Метод проверен вживую ДО спеки.** Сшивка двух пакетов через общий якорь
    дала по контрольному ключу расхождение 0.18 % (28.17680 против 28.12500).
    Оба боевых ответа Google лежат фикстурами в `tests/fixtures/demand/`, поэтому
    тест сшивки стоит на реальных данных и сети не требует.
  - **Главное поведение вехи — различение «ноль» и «не знаем».** Широкий якорь
    раздавливает нишевые запросы: Google отдаёт им нули с `has_data false`.
    Такой ключ едет `null` с причиной «ниже разрешения якоря», а измеренный ноль
    остаётся `0.0`. Проверено на живом Google, а не только тестом.
  - **Перекрёстный прогон Grok: 23 мутации, выжили три** — и все три оказались
    дырами в ТЕСТАХ, а не в коде: «первый пригодный пакет якоря» нигде не
    проверялся, три оговорки сверялись по `len == 3`, дефолт окна `today 12-m`
    не читал ни один тест. Каждого выжившего я перепроверил своей рукой перед
    тем, как отдавать в правки: под каждой мутацией набор оставался зелёным.
  - **Заход исправлений один, и он был только в тестах.** Боевой код после него
    байт в байт прежний (сверены sha256 четырёх файлов). Обвязка мутаций выросла
    с 7 до 10 — три бывших выживших закреплены в ней, чтобы дыры не открылись
    заново. Приёмка: гейт 9/9 с перезапуском команд, 602 passed на 3.12 и 3.14.
  - **Замечено за скоупом (в бэклог, не чинилось):** значение, посчитанное по
    одной измеренной неделе из 53, выглядит в столбце так же уверенно, как
    посчитанное по 53 из 53 — покрытие видно только в `measured_weeks`.
    Кандидат на порог достоверности в следующей вехе.

- **Ветка `m11-live-integration-tests` разобрана и погашена.** Мержить было
  нечего: её работа уже в `main` побайтно — `tests/test_live_providers.py`
  (134 строки) и `docs/specs/m11-live-integration-tests.md` идентичны
  веточным. Единственное расхождение — `AGENTS.md`: ветка держала ВРЕМЕННЫЙ
  клоновый оверрайд с границами того прогона («Docker запрещён», «ровно один
  коммит»), который `main` осознанно вернул к проектным правилам коммитами
  `40a8a2d` и `8127f6b`. Слить ветку значило откатить `AGENTS.md` на чужие
  временные рамки — поэтому ветка удалена без merge-коммита (тип `db7b2db`,
  восстановима из reflog, пока он жив). Это и был тот самый «конфликт с main».
  - Проверка перед удалением: полный CI-набор в Docker на 3.14 — ruff, format,
    mypy, `554 passed, 4 deselected`; живой прогон `pytest -m integration -q`
    в тот же контейнер — `4 passed` против настоящего Google (autocomplete и
    trends, кредов не требуют). То есть работа M11 не просто лежит в `main`,
    а работает.
  - Побочно закрыт пробел в `AGENTS.md`: файл описывал обычный прогон и
    мутационный гейт, но ни словом не упоминал маркер `integration` и четыре
    живых теста — их можно было не заметить или «починить» `addopts`,
    отвязав CI от сети наоборот.

## COMPLETED (2026-09-05)

- **M16 — потолок страниц Google Ads и честная обрезка.** Исполнитель Grok,
  работал прямо в дереве; принято и влито в `main` (`6b0b792`). Настройка
  `GKAI_GOOGLE_ADS_MAX_PAGES` (дефолт 20) в существующем валидаторе,
  `KeywordIdeaPage(ideas, truncated, truncation_reason)` по образцу
  `SearchAnalyticsPage`; обрезанный ответ НЕ кешируется (иначе неделю выдавался
  бы как полный); `run_ads_ideas` на обрезке отдаёт `completeness=partial`,
  `CompetitorResearch` — предупреждение. Приёмка: 548 passed на 3.12 и 3.14
  (было 532), ruff/format/mypy чисто.
  - Приёмочная мутация вскрыла ДЫРУ В ТЕСТАХ, которую пришлось закрыть самому
    (`6b0b792`): проверки `next_page_token` и `pages_read >= max_pages` шли в
    правильном порядке, но ни один тест не отличал их перестановку — не было
    случая «пейджер кончился РОВНО на потолке». С переставленными проверками
    полный ответ из ровно `max_pages` страниц объявлялся бы обрезанным и
    навсегда переставал кешироваться, а весь набор оставался зелёным. Тест
    `test_last_page_at_the_cap_is_not_truncated` закрывает границу.
  - Замечено за скоупом (не чинилось): ключ кеша не включает
    `google_ads_max_pages`; `historical_metrics` пейджера не имеет.

- **M15 — Search Console: quota project и разбор 403.** Спека
  `docs/specs/m15-gsc-quota-project.md`, исполнитель Codex, забрано патчем
  (`a1335f8`). Настройка `GKAI_SEARCH_CONSOLE_QUOTA_PROJECT_ID` + валидатор на
  пустое значение; `_load_with` применяет `with_quota_project` к обоим типам
  кредов; 403 с `reason: accessNotConfigured` стал `InvalidConfigurationError`
  вместо отказа аутентификации. До вехи путь `authorized_user` не работал
  вообще — найдено первой же живой проверкой (разрешение владельца, TG 9969:
  использовать его gcloud-ADC, read-only; файл в репозиторий не попадает и
  передаётся только переменной окружения).
  Приёмка: Docker 3.12 и 3.14 по 532 теста зелено (было 496, +36 новых),
  ruff/format/mypy чисто; 4 мутанта из 4 убиты на НОВЫХ тестах отдельно от
  старых; живой `gkai gsc properties` с quota project вернул 95 свойств и
  `completeness: complete`, без него — внятную причину про quota project
  вместо «authentication failed».
  **Прогон дважды оборвался на `Selected model is at capacity`** — оба раза не
  по контракту и не по вине работы: код и тесты дописаны, но финального
  JSON-отчёта исполнитель не отдал, поэтому машинный гейт вердикта дать не мог
  и приёмку я делал целиком руками. Урок: оборванный на capacity прогон стоит
  принимать по факту работы, а не гонять третий раз; гейт — не единственный
  слой приёмки, а последний из машинных.

## COMPLETED (2026-09-04)

- **M14 — верхняя граница объёма кеша.** Спека `docs/specs/m14-cache-size-limit.md`,
  исполнитель Codex, забрано черри-пиком `b75738b` (только рабочий коммит, без
  служебного блока обвязки). Миграция №4 (`last_read_at` + индекс + backfill),
  `SqliteCache.evict_over_limit()` за один проход парой `(key, last_read_at)`,
  настройка `GKAI_CACHE_MAX_BYTES`, поле `max_bytes` в `gkai cache status`.
  Приёмка: Docker 3.12 и 3.14 по 496 тестов зелено; 14 мутантов (7 из спеки +
  7 своих) убиты; живой прогон миграции базы версии 3 с данными и четыре живых
  `gkai expand` под потолком 20 000 байт.

- **M13 — просроченный кеш перестаёт накапливаться.** Спека
  `docs/specs/m13-cache-retention.md`, исполнитель Codex, влито в `0db0f82`.
  `SqliteCache.purge_expired/purge_all/counts`, подметалка при первой записи,
  команды `gkai cache status` и `gkai cache purge [--all] [--vacuum]`.

- **M11 — живые опт-ин тесты неофициальных провайдеров.** Спека
  `docs/specs/m11-live-integration-tests.md`, исполнитель Grok, влито в
  `8127f6b`. `tests/test_live_providers.py` ходит в настоящий Google;
  запускать явно: `pytest -m integration` (в дефолтный прогон не попадает).
- **M12 — одновременный старт не роняет открытие базы.** Спека
  `docs/specs/m12-concurrent-migrations.md`, исполнитель Codex, влито в
  `40a8a2d`.

## Что стоит знать следующей сессии

- **CI здесь стреляет на КАЖДЫЙ пуш, но минут не ест.** В `.github/workflows/ci.yml`
  стоит голый `on: push` без фильтра веток, матрица 3.12/3.14 — то есть два job'а
  на любой пуш, включая пуш одной строки в `TASKS.md`. Расход при этом нулевой:
  репозиторий публичный, а стандартные раннеры на публичных репо не биллятся.
  Проверено у самого GitHub, а не по памяти:
  `gh api /repos/stufently/google-keyword-ai-mcp/actions/runs/34058372449/timing`
  → `billable.UBUNTU.total_ms = 0` при `run_duration_ms = 51000`. Временное
  правило владельца про экономию минут Actions этот репозиторий не задевает;
  если репозиторий когда-нибудь станет приватным, счёт начнётся с этого же
  голого `on: push`.

- **Границы вехи сверять с тем, что веха ЛОМАЕТ, а не только с тем, что она
  меняет.** Спека M14 не пустила исполнителя в `tests/test_storage.py`, а
  добавленная колонка `last_read_at` уронила тамошнюю сверку набора колонок на
  равенство. Исполнитель починить не мог, увидеть — тоже (в его песочнице
  висел агрегирующий прогон). Частный случай общего правила: **добавление
  колонки ВСЕГДА задевает тест, сверяющий набор колонок на равенство.** То же
  самое было в M12 (спека запрещала трогать файл, где лежала половина
  причины) — значит, это не случайность, а системная ошибка при написании
  спек.
- **Урок M12/M14 закрыт в процессе, а не только записан.** 05.09.2026 в скилах
  `codex-build` и `grok-build` (живые копии + источник правды `~/github/ansible`,
  коммиты `821c1fa` и `bb86a8d`): в `preflight.sh` починена проверка «веха меняет
  схему, а в дереве лежит запрещённый equality-тест», в шаблон спеки добавлена
  команда поиска таких тестов, в наборы скилов — регрессионные случаи.
  **Проверка МОЛЧАЛА на M14 — том самом случае, ради которого написана:** она
  искала кандидатов только по сырому SQL (`PRAGMA table_info`), а
  `tests/test_storage.py` читал колонки инспектором SQLAlchemy
  (`inspect(engine).get_columns(...)`). Регрессионный тест к ней носил имя
  настоящего теста M14, но был написан сырым SQL — то есть набор был зелен и
  при дырявой проверке. Мораль на будущее: **гард, написанный из конкретного
  провала, проверяй ПРОГОНОМ на этом самом провале, а фикстуру пиши копией
  боевого кода, а не пересказом по памяти.**
- **Мутационная проверка «откатим всё разом» ничего не доказывает.** По этой же
  правке я отчитался «откат шаблона убивает 7 из 11 тестов» — звучит как
  покрытие, а на деле это ОДИН мутант. Четвёртый круг ревью (Codex + Grok,
  оба «НЕ ПРИНИМАТЬ») потребовал поштучного отката: из пяти отдельных правок
  набор держал ОДНУ, остальные четыре мутанта выжили. Правило: **мутант — это
  одна правка, а не весь дифф**; на каждую добавленную форму нужен свой кейс.
  Заодно нашлась вторая половина болезни: расширяя шаблон, я добавил ложные
  срабатывания (`assertGreaterEqual`, `assertNotEqual`, `toBeGreaterThanOrEqual`,
  сравнение диалекта), а у предупреждения есть потолок выдачи — мусор ВЫТЕСНЯЕТ
  настоящий файл. Сейчас 12 мутантов поштучно, 12 убиты.
- **Дыра в тестах M13 закрыта.** Три мутанта, пережившие набор M13
  (игнорировать `cache_sweep_enabled`; подмести ДО проверки `cache_enabled`;
  убрать `logger.warning` из best-effort), на наборе M14 умирают. Приём,
  который сработал: у критерия РОВНО ОДНО утверждение и ровно один тест, а
  просроченную запись кладут в базу ДО первого `set()`, а не после.
- **Мутационная проверка чужих тестов — не формальность, но и не приговор.**
  В M11 и M12 выживали мутанты, в M13 выжили три; в M14 умерли все 14, включая
  семь, которых в спеке не было. Разница — в том, что критерии M14 писались по
  одному утверждению на критерий.
- **Один критерий — одно утверждение.** Связка нескольких проверок через
  `-k "a or b or c"` даёт зелёный, когда сработала хотя бы часть, и гейт
  этого не видит (ложный AC-005 в M13).
- **Приёмка ловит то, чего не ловит гейт.** У M14 гейт дал 11/12 и упёрся в
  зависший в песочнице in-memory MCP-вызов, а настоящий регресс (красный
  `tests/test_storage.py`) нашёлся только полным прогоном в Docker. У M11 гейт
  отверг отчёт из-за формы, у M12 исполнитель честно остановился по контракту.
  Работа была годной во всех случаях, но доводил её принимающий.
- **У WAL-половины гонки M12 надёжного теста нет, и это записано честно.**
  Многопроцессный тест ловит возвращённую поломку примерно в половине случаев;
  прицельный тест на механизм не поймал её вовсе и был удалён. Доказательство —
  замер в комментарии к тесту: с дефектом 3 падения из 20, после — 0 из 25.
  Если кто-то придумает детерминированный способ воспроизвести отказ
  `PRAGMA journal_mode=WAL`, это будет настоящим улучшением.

## Кандидаты на следующие вехи

Ни один не исследован до уровня спеки — перед запуском обязателен шаг 1
маршрута (проверить предпосылки вживую), иначе спека унаследует чужую догадку.

- Пагинация Google Ads не ограничена: `_call_ideas` вычерпывает весь pager под
  лимитом 1 rps, а `max_ads_calls` считает это одним вызовом. Задокументировано
  как осознанное решение в `docs/pipeline.md`, так что сначала решение, потом
  спека. **Половина шага 1 сделана по коду 05.09.2026** (живая часть требует
  кредов Ads, которых нет):
  - Провайдер про бюджет не знает вовсе — слова `budget` в
    `providers/google_ads.py` нет ни разу, и внутри цикла по страницам
    (`_call_ideas`, строки 396–431) не проверяется ни `max_ads_calls`, ни
    `max_runtime_seconds`. Единственное ожидание в цикле — троттлер 1 rps.
  - Отсюда точная формулировка риска: **`max_runtime_seconds` (дефолт 300 с)
    не может прервать обход пейджера.** Гард смотрится МЕЖДУ стадиями, а обход
    N страниц стоит ~N секунд внутри одного учтённого вызова. Это не «неточный
    учёт», а дыра в ограничении времени: одна затянувшаяся выдача съедает весь
    бюджет прогона, и остановить её нечем.
  - Чего по коду узнать НЕЛЬЗЯ и что решает величину проблемы: сколько страниц
    реально отдаёт `generateKeywordIdeas` на боевых запросах. При странице в
    1000 идей (`google_ads_page_size`) правдоподобны единицы страниц — тогда
    это секунды и веха не нужна; если десятки-сотни — нужна. **Замерить можно
    только вживую, то есть после кредов Ads.**
